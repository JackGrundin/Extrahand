# FastGig – App för flexibla jobb och uppdrag

## Vad appen gör
Plattform som kopplar samman företag och privatpersoner för kortare jobbpass och längre uppdrag. Företag publicerar jobb med datum, tider, adress, timlön och OB-tillägg. Privatpersoner ansöker, chattar med företag och får betyg efter avslutat pass. Det finns även en schemafunktion för längre uppdrag som sommarjobb och säsongsarbete.

## Viktiga detaljer
- Alla fält i jobbformuläret är obligatoriska utom OB-tillägg (titel, beskrivning, kategori, stad, adress, timlön, antal dagar, datum+tider per dag). Delad validering i frontend/src/utils/jobbValidering.js (valideraJobb) + inline-fel via components/FältFel.js. Backend är sista försvaret: routes/jobb.js valideraJobbInput körs i både POST och PUT och avvisar bl.a. tom/0 timlön med 400 – annars ger jobbet en tidrapport på 0 kr
- Admin-vy tillgänglig för info@fastgig.se med flikar: Tidrapporter, Avtal, Företag, Fakturering
- Faktureringspris beräknas: timlön + timlön*0.32 + timlön*0.06 + ((timlön*1.32) + (timlön*1.32*0.06)) * påslag
- OB-fakturering: samma formel med ob_belopp i stället för timlön
- Påslaget är 0.20 (Pro-kunder + gratiskontonas två första pass per månad) eller 0.40 (från 3:e passet). Formeln bor i backend/utils/pris.js och speglas i frontend/src/utils/konstanter.js – ändra alltid på båda ställena
- Påslaget FRYSES på jobbet (Jobb.paslag) när en ansökan GODKÄNNS – inte vid publicering – och kopieras sedan till tidrapporten. Tas godkännandet tillbaka nollas påslaget och räknaren minskas: ett pass som aldrig blev av ska inte kosta något. Jobb utan påslag faktureras med 40%
- Två skilda storheter: pass_denna_manad räknar GENOMFÖRDA (godkända) pass och styr påslaget, medan popupen vid publicering styrs av antalet jobb företaget publicerat denna månad (räknas direkt ur Jobb, ingen kolumn – så ett borttaget jobb försvinner automatiskt ur räkningen)
- Planvals-popupen visas högst EN gång per månad. Väljer företaget "Fortsätt utan abonnemang" stämplas planval_manad ('YYYY-MM') och de slipper frågan resten av månaden. Stämpeln är självläkande: avser den en tidigare månad räknas valet som ogjort, så popupen kommer tillbaka vid månadsskifte utan cron
- Ett jobb = ett pass och räknas EN gång. Godkännandevägen är inte idempotent (företaget kan godkänna, återkalla, byta person), så räkningen villkoras på om jobbet redan hade en godkänd ansökan – se hämtaGodkändaFörJobb i routes/ansokningar.js
- Prenumeration: Pro 299 kr/mån via Stripe Checkout (extern webbläsare) + Stripes kundportal. Webhook på /webhooks/stripe måste monteras med express.raw FÖRE express.json, annars går signaturverifieringen inte att göra
- Pass-räknaren (pass_denna_manad + månadsstämpeln pass_manad) nollställs av cron den 1:a, men databasfunktionerna oka_/minska_pass_denna_manad nollställer också lazily vid månadsskifte – så en omstartad Railway-process kan inte råka fakturera 40% i en ny månad
- Privatpersoner blockeras från att ansöka tills avtal är godkänt (avtal_godkant = true)
- BEHÖRIGHETSKRAV (behorighets_krav jsonb, en array av strängar) finns på BÅDE Jobb och scheman, och scheman.behorighets_krav MÅSTE speglas till annons-jobbet av synkaAnnonsJobb i db/scheman.js – spärren sitter i POST /api/ansokningar/:jobbId och läser Jobb-raden, så utan speglingen är ett schemas krav helt verkningslösa (samma miss som Typ hade). Att spärren ligger just där är hela poängen: en schemaansökan går mot annons-jobbet, alltså genom samma route som ett enstaka pass, så EN kontroll täcker båda flödena. Ansökan fryser JOBBETS kravlista i ansokningar.intygade_krav (+ intygat_at), aldrig den lista klienten skickade – klientens lista används bara för att kontrollera att allt kryssats i. intygade_krav är nullbar UTAN default och skillnaden bär: NULL = ansökan från före funktionen fanns, [] = jobbet hade inga krav. Reglerna bor i backend/utils/behorighet.js (normaliseraKrav, valideraBehorighetsKrav, saknadeKrav) och speglas av frontend/src/utils/behorighet.js – ändra alltid på båda ställena
- Företaget får lägga till krav även efter att ansökningar kommit in. Då skickar notifieraOmNyaKrav (utils/kravNotis.js, delad av PUT /api/jobb/:id och PUT /api/scheman/:id) en push till alla icke-avvisade sökande, och de bekräftar via POST /api/ansokningar/:id/intyga. Att TA BORT ett krav notifierar aldrig – färre krav kan inte göra en ansökan ogiltig, och saknadeKrav itererar därför jobbets krav, inte de intygade. "Kräver ny bekräftelse" är HÄRLETT ur saknadeKrav och aldrig ett lagrat statusvärde: brickan ska försvinna av sig själv när personen bekräftar. Jämförelsen sker på trimmad exakt sträng, så en omskrivning av ett krav räknas som nytt – rätt riktning att fela åt när texten är det enda som beskriver kravet
- E-postverifiering via Resend (noreply@fastgig.se) krävs vid registrering
- GLÖMT LÖSENORD: "Glömt lösenord?" på LoggaInScreen → GlömtLösenordScreen → POST /api/auth/glomt-losenord, som skickar en länk via Resend. Endpointen svarar ALLTID `{ ok: true }`, även för okända adresser och även när mejlet fallerar – ett svar som skiljer på "finns" och "finns inte" gör den till ett verktyg för att kartlägga vilka som har konto. Kvittensen i appen måste därför vara formulerad likadant i båda fallen. Bara SHA-256-hashen av token lagras (aterstallning_token_hash), aldrig klartext: token räcker för att ta över kontot, precis som lösenordet. Länken är giltig i en timme och bränns i samma update som lösenordet sätts, så den kan inte återanvändas. Lösenordet byts på en WEBBSIDA som backend serverar (routes/aterstallningSida.js, monterad på `/` utanför /api), inte i appen – appen har inget deep link-schema, och webbsidan fungerar även när mejlet läses på en dator. Sidan postar JSON via fetch, så ingen express.urlencoded behövs; token saneras till hex innan den ekas in i script-taggen. MIN_LÖSENORD_LÄNGD i utils/losenord.js delas av sidan och API:t – annars släpper sidan igenom lösenord som servern avvisar. Lyckad återställning sätter även email_verifierad, annars fastnar en overifierad användare med rätt lösenord men blockerad inloggning
- TA BORT KONTO (krav från App Store, DELETE /api/users/konto): användarraden RADERAS ALDRIG. Tidrapporter, fakturaunderlag, betyg och chattmeddelanden pekar på id:t, så en borttagen rad skulle slita sönder både bokföringen och motpartens historik – utfört arbete ska gå att betala ut även efter att personen lämnat. I stället nollas varje personuppgift, `aktiv` sätts till false och raderad_at stämplas. E-posten ersätts med `raderad+<id>@fastgig.se` i stället för NULL: kolumnen är unik, och personen ska kunna registrera sig på nytt med sin riktiga adress. Lösenordet sätts till 'RADERAT' så bcrypt.compare alltid faller. Väntande ansökningar avvisas (godkända rörs aldrig), och profilbilden tas bort ur Storage separat – den ligger utanför databasen. Testa ALLTID `aktiv === false`, aldrig `!aktiv`: NULL betyder konto från före kolumnen fanns. Inaktiva konton filtreras bort i hämtaAllaJobb och hämtaÖppnaScheman (via foretagAktiv från berikaMedPassOchFöretag) samt i admin-listorna – annars ligger annonser kvar från företag som varken kan svara eller godkänna. Appen loggar ut direkt efter raderingen; JWT:n i telefonen gäller annars tills den går ut
- INGEN INTERNETANSLUTNING: klient.js normaliserar alla nätverksfel till ett Error med `kod: INGEN_ANSLUTNING` och texten i INGEN_ANSLUTNING_TEXT, så att skärmarnas vanliga `Alert.alert('Fel', fel.message)` säger rätt sak utan att någon skärm behöver ändras. fetch kastar BARA vid nätverksfel eller avbrott, aldrig på HTTP-statuskoder – ett 500-svar ska alltså behålla serverns eget felmeddelande. AbortController ger 20 s timeout (Promise.race hade lämnat anropet hängande i bakgrunden). Statusen kommer från de riktiga anropen, inte från @react-native-community/netinfo: det är servern vi bryr oss om (telefonen kan ha wifi utan att komma vidare), och en native-modul hade krävt en ny app-build. AnslutningsContext lyssnar via lyssnaPåAnslutning och pingar /api/health var 5:e sekund medan vi är nere, annars ligger OfflineBanner kvar tills användaren råkar utlösa ett anrop. SafeAreaProvider ligger numera i App.js för bannerns skull – navigatorerna har egen fallback och påverkas inte
- Backend körs på Railway via api.fastgig.se
- Realtidsuppdateringar via Supabase Broadcast (meddelanden, tidrapporter, ansökningar, betyg, jobbförfrågningar, scheman)
- PostgREST skickar .in()-listor i URL:en och har en längdgräns. Ett schema ger en ansökan per pass, så listorna växer snabbt – använd hämtaIBitar i db/ansokningar.js i stället för att skicka rå .in() med tusentals id
- Supabase-tabeller: användare, Jobb, ansokningar, meddelanden, betyg, tidrapporter, faktureringsunderlag, jobbforfragan, scheman, schema_pass, schema_avdrag
- Jobb försvinner från listan när någon blivit godkänd eller datumet passerat
- Bestridandeflöde: privatperson bestrider → förklaring sparas → företag skickar korrigerad tidrapport
- Schemafunktion: företag skapar schema i ett FYRSTEGSFLÖDE (PubliceraSchemaScreen med StegIndikator): 1 grunduppgifter, 2 period + kalender där man klickar i passdatum med veckodagsgenvägar (Mån–Sön, Vardagar, Helger), 3 detaljer per pass – tryck på raden fäller ut en editor i den, kryssrutan markerar den för massredigering, 4 löneavdrag + publicering. SchemaPassModal finns kvar för finjustering av ett enskilt pass. Perioden i steg 2 är BARA ett UI-filter för kalendern – den skickas aldrig till servern, som härleder startdatum/slutdatum ur min/max av passens datum (härledPeriod). EN person söker och godkänns för hela schemat. Guiden har en beforeRemove-vakt som gör Android-bakåt/svep till "ett steg bakåt" i stället för att lämna skärmen – den fångar ALLA bort-navigeringar, så varje avsiktlig utgång måste sätta harPublicerat-refen först (annars backar man bara till steg 3). Efter publicering: navigate till MinaJobbTab med flik 'scheman' + popToTop, eftersom ett flikbyte inte avmonterar guiden och det publicerade schemat annars ligger kvar ifyllt
- Passlogiken i det stegvisa flödet bor i frontend/src/utils/schemaPass.js, utanför skärmen så att den går att testa utan React Native. Fyra regler är lätta att få subtilt fel: (a) synkaPassMotDatum SLÅR IHOP valda datum med befintliga pass så att ifyllda tider överlever att man går tillbaka till steg 2 – bygg aldrig om listan från grunden; (b) varje pass får ett lokalt id och det öppna passet pekas ut med id, inte index, eftersom listan sorteras om när tider fylls i; (c) tillPayload strippar det lokala id:t innan det skickas till servern; (d) nyttPassId() räknar upp en modulnivåräknare, så synkaPassMotDatum får ALDRIG anropas bara för att räkna – vill man veta antalet pass finns antalPassEfterSynk, som är bieffektsfri. Antalet valda DATUM är för övrigt inte antalet PASS: en dag kan ha flera pass
- MASSREDIGERING I STEG 3 har EXAKT ETT URVALSLÄGE åt gången, och det är hela poängen: markerade-Set:et och öppetPassId får aldrig vara satta samtidigt. markeraPass nollar öppetPassId, öppnaPass rensar markeringen, och finns redan en markering togglar radtrycket kryssrutan i stället för att fälla ut editorn. Massredigering fanns här förut och togs bort för att två urval i samma vy gjorde steget rörigt – regeln ovan är vad som gör att den kan finnas igen. MassPassPanel håller sitt eget utkast med TOMMA fält, aldrig förifyllda från något markerat pass: tillämpaPåMarkerade i schemaPass.js skriver bara ifyllda fält, så "sätt bara rollen" får inte nolla passens redan ifyllda tider. Ett tomt ob_tillagg betyder "rör inte OB" och går därför inte att skilja från "ta bort OB" – därav den explicita rensaOb-flaggan och länken "Ta bort OB på markerade pass". Panelen ligger sist i flex-kolumnen, INTE absolut positionerad: absolut placering la den över stegnavigeringen och stängde in användaren. TidVäljares rördes-flagga är kritisk här – utan den skriver "öppna och tryck Klar" 08:00 till alla markerade pass på en gång. Markeringen rensas vid varje stegbyte, även StegIndikatorns hopp bakåt

- hittaKrockar fångar två pass med samma datum + starttid när en dag har två roller – backend avvisar det med 400, men utan kontrollen syns felet först vid publicering. harNolltid blockerar identiska tider; blockera ALDRIG "sluttid före starttid", pass över midnatt (22:00–06:00) är giltiga och hanteras av slutEpochFörPass
- MånadsKalender har två lägen: läsvy (valtDatum = en dag, rollprickar) och flerval (valdaDatum = Set, minDatum/maxDatum släcker datum utanför perioden). Flervalet är additivt – rör inte valtDatum-vägen eller prickfärgslogiken, SchemaKalenderScreens förklaringsrad är beroende av dem Automatisk tidrapport skapas när varje pass sluttid passerar. Privatperson godkänner eller bestrider som vanligt i chatten. Kalendervy nås via företagets profil (Schemaöversikt), visar vem som jobbar vilka dagar och grupperar dagens pass per roll. Rollprickarna har TRE tillstånd i prioritetsordning: ring = någon i rollgruppen saknar person, nedtonad fylld = allt genomfört, fylld = bemannad och planerad (ett genomfört pass är alltid bemannat, så de två första kan inte krocka). Sorteringen i rollerFörDag (MånadsKalender.js) och grupperaPerKategori (SchemaKalenderScreen.js) MÅSTE vara identisk – namnlösa sist – annars radar prickarna och dagslistan upp rollerna olika. hämtaKalenderPass returnerar även OBEMANNADE pass med flit; sammanfattningen räknar därför bemannade och väntande dagar var för sig, och en delvis bemannad dag räknas i båda. Kalenderns passkort öppnar SchemaDetalj, så SchemaDetalj OCH RedigeraSchema måste vara registrerade i ProfilNavigator
- KATEGORI OCH OB PER PASS: schema_pass.kategori (fri text, företagets egna avdelningar som "Liftvärd" – INTE ur KATEGORIER-listan) och schema_pass.ob_tillagg. Båda är NULL-bara UTAN default, och den skillnaden är bärande: NULL = ärv schemats värde, `[]` = passet har medvetet inget OB. Med `default '[]'` hade alla publicerade scheman tyst tappat sitt OB. Arvet sker på EXAKT ett ställe – passMedArv i db/scheman.js – som anropas av schemaTilldelning, cron/schemaTidrapport, GET /scheman/:id, hämtaKalenderPass och hämtaSchemaPassFörAnvändare. Lägg aldrig till egna if-satser för bakåtkompatibiliteten
- SCHEMATYP: företaget väljer typ i steg 1 (obligatoriskt, inget förval) och kan ändra den efteråt i RedigeraSchemaScreen, ur SCHEMATYPER i frontend/src/utils/konstanter.js, speglad av GILTIGA_TYPER i backend/routes/scheman.js – ändra alltid på båda ställena. synkaAnnonsJobb MÅSTE spegla Typ till annons-jobbet; raden saknades ända tills typen blev redigerbar och jobbet hade annars behållit sin ursprungliga typ vid varje byte. Typbyte skickar ingen notis (till skillnad från timlön) – det är en etikett för sökbarhet och ändrar inget om arbetet. Lagrade värden är ASCII-slugar ('sasongsarbete', inte 'säsongsarbete') som resten av typvärdena i databasen; etiketten visas via schematypEtikett, som faller tillbaka på 'Schema' och ALDRIG visar råvärdet – gamla scheman kan bära 'gig', 'deltid', 'heltid' eller 'uppdrag' som den tidigare valideringen tillät. Varken scheman.typ eller Jobb.Typ har check-constraint, så nya typvärden kräver ingen migration. Privatpersoner filtrerar på typ med flerval i JobbScreens schemaläge; Gig/Sommarjobb-filtret för enstaka pass är borttaget
- Ett schema har INGEN huvudkategori längre – rollen sätts per pass. `scheman.kategori` finns kvar i databasen (nullbar) för scheman skapade när fältet var obligatoriskt; deras pass utan egen roll ärver den via passMedArv. Tas kravet bort någon gång även i PUT måste `kategori?.trim()` behållas i routes/scheman.js – utan optional chaining kastar raden TypeError och ger 500. Huvudkategorin nådde ALDRIG jobbfiltret: hämtaAllaJobb/hämtaJobbFörFöretag/hämtaTidigareJobbFörFöretag filtrerar bort schemajobb med `.is('schema_id', null)`, och schemalistans filter i JobbScreen går bara på stad
- Rollen visas via `rollFärg` i frontend/src/utils/konstanter.js, som ger varje rollnamn en deterministisk färg ur en palett på åtta. Samma färg används i kalenderns prickar, i förklaringsraden ovanför kalendern, i dagslistans rubriker och i RollBrickor – ändras hashen tappar de vyerna sin koppling. Hashen är FNV-1a med avalanche-steg av ett skäl: en enkel `hash * 31`-summa fördelar sig uselt mot åtta färger eftersom 31 ≡ −1 (mod 8). Färgen är aldrig enda signalen – rollnamnet står alltid i klartext
- API:t levererar `kategorier` (schemats roller, VANLIGAST FÖRST via räknaKategorier i db/scheman.js) på GET /api/scheman, /mina och /:id. RollBrickor visar de tre första och faller tillbaka på en neutral "Schema"-bricka när schemat saknar roller
- LÖNEAVDRAG (schema_avdrag: namn, belopp, typ per_dag|totalt, aktiv) påverkar ALDRIG faktureringsbeloppet – företaget faktureras på bruttot precis som förut, och avdraget reglerar bara vad personen får ut. `totalt_belopp` på tidrapporten förblir brutto; nettot är `totalt_belopp - avdrag_belopp`. Avdragen FRYSES på tidrapporten (kolumnerna avdrag jsonb + avdrag_belopp) eftersom fakturaunderlaget produceras uteslutande ur tidrapporter – annars kunde en redigering i september ändra en faktura från juli. typ='totalt' fördelas jämnt (belopp / antal pass) och kvoten avrundas ALDRIG till hela ören – med avrundning blir summan över passen inte det företaget skrev in (5000/14 → 357,14 × 14 = 4999,96). avdrag_belopp är numeric utan precision, så databasen avrundar inte heller. Totalen i UI får aldrig räknas fram genom att multiplicera tillbaka avdraget per pass; använd beräknaAvdragTotalt i konstanter.js, som utgår från de inskrivna beloppen. Beloppslogiken bor i beräknaBelopp/beräknaAvdragFörPass i utils/pris.js, speglade i konstanter.js
- Avdrag allokeras bara EN gång per pass: en korrigerad rapport efter bestridande är en ny rad för samma ansökan, så POST /api/tidrapporter KOPIERAR avdrag från föregående rapport i stället för att räkna om. PATCH /korrigera rör inte avdragen alls. beräknaBelopp klampar avdraget mot bruttot så att ett stort avdrag på ett kort pass aldrig ger negativ utbetalning
- Avdrag måste synas för den sökande INNAN hen ansöker (SchemaDetaljScreen, ovanför ansökningsknappen). Ett löneavdrag som dyker upp först på tidrapporten är fel ordning. Avdrag läggs till OCH tas bort i steg 4 när schemat skapas och därefter i RedigeraSchemaScreen. SchemaDetaljScreen bara VISAR dem, och numera enbart för den sökande – företaget har ingen avdragslista där alls. Samma sak med "Visa på karta": den är villkorad på !ärFöretag, eftersom vyn delas av båda användartyperna och företaget känner sin egen adress
- BACKEND ÄR SISTA FÖRSVARET, och en granskning visade att schemafunktionen litade på frontend på fyra ställen. valideraPassLista i routes/scheman.js (delad av publicering OCH POST /:id/pass) avvisar nu: passdatum före dagens, identiska start- och sluttider, och fler än MAX_ANTAL_PASS (200, speglat i PubliceraSchemaScreen.js). Datumregeln är en pengafråga, inte kosmetik: hämtaPassAttTilldela filtrerar `.gte('datum', idag)` och cron kräver ansokan_id, så ett passerat pass blir permanent dött – men räknas ändå av räknaPassFörSchema, som är divisorn i beräknaAvdragFörPass, och späder därmed ut löneavdraget så att summan aldrig når det inskrivna beloppet. En tidrapport får dessutom bara besvaras EN gång (status måste vara 'väntar'): utan vakten kunde en redan fakturerad rapport bestridas i efterhand, eftersom fakturaunderlaget plockar rapporter med status 'godkänd'
- ADRESS OCH STAD hör ihop och står alltid intill varandra i de fyra formulären (publicera/redigera jobb och schema): StadInput följt av AdressInput med stad={plats}. Staden MÅSTE väljas ur SVENSKA_ORTER överallt – fri text splittrar stadsmatchningen så att jobbnotiser missar folk, och undantaget i RedigeraJobbScreen (kräverStadFrånLista: false) är borttaget. Adressen tillåter däremot fri text: en nybyggd adress eller "Norra grinden vid lastkajen" måste gå att skriva, och ett tvingat val skulle blockera publicering när tjänsten inte känner till arbetsplatsen. Förslagen kommer från Nominatim (OpenStreetMap) via backend-proxyn GET /api/adress/sok – ALDRIG direkt från appen: Nominatims villkor kräver en identifierande User-Agent och ungefär max ett anrop per sekund (därav 400 ms debounce i AdressInput och minnescache i routen), och proxyn gör ett framtida byte till Google Places till en ändring i routes/adress.js utan att någon skärm rörs. Proxyn returnerar tom lista i stället för 500 vid timeout eller nedtid, så fältet degraderar till vanlig fri text. Staden fylls ALDRIG i automatiskt från ett valt adressförslag: Nominatims ortnamn följer inte SVENSKA_ORTER, så ett automatiskt "Åre kommun" hade gjort formuläret osparbart
- OB-tillägg som kommer från klienten MÅSTE gå genom valideraObTillagg i utils/pris.js. beräknaObBelopp gör ob.start.split(':') och kastar på trasig data; cron/schemaTidrapport fångar felet och sätter tillbaka passet till 'planerad', vilket ger en evig omförsöksloop var femte minut
- SCHEMATS BÄRANDE IDÉ: ett schema materialiseras som vanliga Jobb-rader. Annons-jobbet (schema_id satt, schema_pass_id NULL) skapas vid publicering och bär ansökningar, chatt, påslagsfrysning och räkningen mot gratisgränsen. Pass-jobben (båda kolumnerna satta) skapas ETT PER PASS när en person godkänns, vart och ett med en direkt godkänd ansökan – samma mönster som routes/jobbforfragan.js. Därför fungerar chatt, tidrapporter, bestridande, korrigering, betyg och fakturering utan någon ny kodväg, och godkännandet går genom oförändrad kod i routes/ansokningar.js. Se db/schemaTilldelning.js
- REDIGERING EFTER PUBLICERING (RedigeraSchemaScreen, bara i företagets navigator): titel, beskrivning, schematyp, timlön, löneavdrag samt tillägg och inställning av pass. uppdateraSchema tillåter status publicerat OCH tillsatt men aldrig avbrutet. PUT uppdaterar BARA fält som faktiskt skickas – tidigare nollades plats/adress/kategori när de utelämnades, så en redigering av bara titeln raderade adressen. Datum och tider på BEFINTLIGA pass ändras aldrig; ett pass som ska flyttas tas bort och läggs till på nytt. Nya pass på ett tillsatt schema materialiseras genom att anropa tilldelaSchema IGEN – den är idempotent eftersom hämtaPassAttTilldela bara plockar pass med ansokan_id null, så bara de nya får jobb och godkänd ansökan. Skicka alltid schemats redan frysta paslag; det får aldrig räknas om, och pass_denna_manad ska inte öka (schemat är räknat som ett pass). Att "ta bort" ett pass sätter det till installt och avvisar dess ansökan – rader raderas ALDRIG, chattmeddelanden hänger på ansokan_id. Pass med tidrapport avvisas med 409. Ändrad timlön på ett tillsatt schema skickar push: att tyst ändra vad någon får betalt för arbete de tackat ja till är inte acceptabelt. synkaAnnonsJobb tar valfri passlista och speglar då även arbetstider/antal_dagar, vilket krävs efter pass-ändringar eftersom privatpersonens Mina pass-kort läser annons-jobbets arbetstider
- Schemaansökningar är vanliga rader i ansokningar mot annons-jobbet – ingen egen tabell. Det ger chatt redan innan personen godkänts. Räknaren för nya ansökningar återanvänder därför Jobb.ansokningar_sedda_at på annons-jobbet (ingen egen kolumn på scheman) och markeraAnsökningarSedda i db/jobb.js, som är scopead på Foretag_id som annons-jobbet har. berikaMedNyaAnsökningar i db/scheman.js speglar räkneregeln i filtreraAktivaJobb – ändra alltid båda. Tillsatta scheman räknas ALDRIG: ett vanligt jobb lämnar listan när någon godkänts, men schemat ligger kvar, så villkoret anvandare_id == null är det som hindrar badgen från att tjata om bemannade scheman
- Företagets schemakort visar antalet sökande HÖGST UPP, före titeln. Två skilda räknare från berikaMedNyaAnsökningar i db/scheman.js: antalAnsökningar räknar bara status 'väntande' ("så här många kan jag välja bland"), medan nyaAnsökningar räknar ALLA oavsett status för att spegla filtreraAktivaJobb i db/jobb.js – de två måste ge samma svar, annars visar schemabadgen och jobbbadgen olika siffror för samma händelse. Sökandenamnen i SchemaDetaljScreen är klickbara mot SökanadeProfil, som därför också är registrerad i ProfilNavigator (schemat nås därifrån via Schemaöversikt)

- Schema-genererade Jobb får ALDRIG läcka in bland enstaka pass. Filtren ligger i queryn (inte i JS) på sex ställen i db/jobb.js + db/ansokningar.js: hämtaAllaJobb, hämtaJobbFörFöretag (täcker även företagets publika profil), hämtaTidigareJobbFörFöretag, uppdateraJobb/taBortJobb, hämtaPågåendePassFörPåminnelse — alla `.is('schema_id', null)`. räknaJobbDennaMånad filtrerar i stället på `.is('schema_pass_id', null)`, så annons-jobbet räknas som ett publicerat pass men passjobben inte. Filtret är en korrekthetsfråga: i glappet när någon hoppat av saknar framtida passjobb godkänd ansökan och skulle annars bli publikt sökbara annonser
- Auto-tidrapport (cron/schemaTidrapport.js, var 5:e min) och påminnelse dagen före (cron/schemaPaminnelse.js, var 15:e min) är idempotenta via DATABASEN, inte via ett tidsfönster i minnet: krävPassFörRapport flyttar schema_pass planerad→rapporterad och krävPåminnelse stämplar paminnelse_skickad_at, båda i villkorade updates. Bara den körning som vinner agerar, så varken omstart eller samtidiga körningar kan dubblera
- Schemapass rapporteras med SCHEMALAGDA timmar (brutto, ingen rast). Vid övertid eller rast rättar företaget rapporten på plats via PATCH /api/tidrapporter/:id/korrigera, så länge den är auto_skapad och fortfarande 'väntar'. Efter ett bestridande gäller vanliga flödet (ny rapport via POST)
- Hoppar personen av ett schema AVVISAS ansökningarna – de raderas aldrig. Chattmeddelanden hänger på meddelanden.ansokan_id, så radering skulle slita bort historiken. Jobb-raden behålls också och återanvänds av nästa person med sitt redan frysta påslag. Genomförda pass rörs aldrig, och påslag/räknare nollas bara om INGET pass hunnit genomföras
- AVHOPP SYNS BARA VIA ansokningar.avhoppad_at (migrations/ansokan_avhopp.sql, nullbar timestamptz). Statusen förblir 'avvisad' – exakt samma värde som när företaget nekar någon – eftersom statusvärdet styr ett dussin filter i backend (avvisaAllaUtomEn, frigörFramtidaPass, filtreraAktivaJobb) och i appen (STATUSFÄRGER_ANSÖKAN); ett fjärde statusvärde måste speglas överallt och tabellen skapades utanför migrationerna, så en osynlig check-constraint är fullt möjlig. Stämpeln sätts på EXAKT ett ställe: POST /scheman/:id/hoppa-av, via markeraAvhopp i db/ansokningar.js på annons-ansökan plus de id:n frigörFramtidaPass returnerar. återställSchema anropas också när FÖRETAGET tar tillbaka ett godkännande, och det är inget avhopp – stämpla aldrig där. Etiketten kommer från ansökanStatusVisning i konstanter.js ('Hoppat av' för personen, 'Hoppade av' för företaget). MinaAnsokningarScreen filtrerar dessutom bort pass-ansökningar (schemaPassId != null): de skapas automatiskt vid godkännandet och är inget personen sökt, och efter ett avhopp blir varje framtida pass 'avvisad' – utan filtret ett rött kort per pass
- BETYGSPROMPTEN (BetygsContext, monterad i App.js innanför RealtidsProvider) visar BetygModal utanför navigatorn, vilket är det som gör att den kan komma upp var man än står i appen. POST /api/betyg har INGEN kräverTyp längre: båda parter betygsätter varandra, och åtkomstkontrollen i routen släpper ändå bara igenom den som är part i ansökan. Underlaget är GET /api/betyg/vantande → hämtaObetygsatta i db/betyg.js, som grupperar på `schema_id + motpart` och inte per pass – ett schema med 14 pass ger annars 14 prompter för samma person, och motparten måste ingå eftersom en avhoppare kan ha ersatts av någon annan i samma schema. En grupp räknas som klar om NÅGON ansökan i den har ett betyg från mig, annars kom de tretton andra tillbaka direkt efter att man betygsatt. Företaget triggas av den befintliga realtidspingen 'tidrapport'; den som själv godkände får ingen ping till sig själv, så ChattScreen anropar kollaBetyg() efter godkännandet. "Hoppa över" är PERMANENT och sparas bara lokalt i AsyncStorage ('fastgig.betyg.hoppadeOver') – stjärnikonen i chattens header är kvar för den som ändrar sig. Dubbletter spärras numera av ett unikt index på (ansokan_id, av_anvandare_id), inte bara av finnsDublettBetyg

- Tider tolkas alltid som svensk lokaltid via backend/utils/tid.js (delad av alla tre cron-jobben). Använd hourCycle 'h23' i Intl – med hour12:false svarar vissa ICU-versioner timme "24" och ger ett dygns fel i offseten
- Prenumerationssystem: gratis (2 pass/mån med lågt påslag, sedan högre påslag), Pro 299 kr/mån (alltid lågt påslag). Stripe hanterar betalning och prenumerationshantering.

## Tech stack
- Backend: Node.js med Express, REST API
- Databas: PostgreSQL via Supabase
- Frontend: React Native med Expo (mobilapp)
- Auth: JWT-tokens (eget system med bcrypt)
- Hosting: Railway (backend via api.fastgig.se), Supabase (databas)
- Mejl: Resend (noreply@fastgig.se)
- Realtid: Supabase Broadcast
- Betalning: Stripe (prenumerationer)

## Kodstil
- Använd async/await, inte callbacks
- Kommentera på svenska
- Varje fil ska ha tydliga funktionsnamn
- Felhantering på alla API-endpoints

## Mappstruktur backend
/routes     → API-endpoints
/middleware → Auth-kontroll
/db         → Databasanrop
/db/migrations → SQL-migrationer (körs manuellt i Supabase SQL-editorn, ingen runner)
/cron       → setInterval-baserade jobb, startas i server.js
/utils      → pris.js (påslag + beräknaObBelopp), tid.js (svensk tidszon), manad.js, losenord.js (minimilängd delad av API och återställningssidan)

## Mappstruktur frontend
/src/screens    → Alla skärmar
/src/api        → klient.js – alla API-anrop
/src/context    → AuthContext, NotifikationsContext, RealtidsContext, AttAvslutaContext, AnslutningsContext
/src/navigation → Navigationsstruktur
/src/utils      → datumHelper.js, konstanter.js, jobbValidering.js, schemaValidering.js
/src/components → Återanvändbara komponenter (HandlingsKnapp, FältFel, ProBesparing m.fl.)